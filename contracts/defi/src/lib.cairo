pub mod types {
    pub mod defi_types;
}

pub mod tokens {
    // mod defi_types;
    pub mod erc20;
    pub mod jbtc;
}

pub mod defi {
    pub mod vault;
}

/// Module containing tests.
mod tests { // #[cfg(test)]
// mod test_defi;
}
